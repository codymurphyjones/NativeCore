{
  "name": "root",
  "private": true,
  "scripts": {
    "app": "lerna run app --stream",
    "web": "lerna run web  --stream",
    "web:build": "lerna run web:build  --stream",
    "bootstrap": "lerna bootstrap",
	"postbootstrap": "package_scripts copyToMobile.js",
    "clean": "lerna clean",
    "clean:build": "lerna exec -- rimraf lib",
    "link": "lerna link",
    "release": "lerna publish",
    "ls": "lerna ls",
    "postinstall": "package_scripts dependents.js && lerna bootstrap && package_scripts copyToMobile.js"
  },
  "devDependencies": {
    "copy-dir": "^1.0.0",
    "lerna": "^3.13.4"
  },
  "dependencies": {
    "@mager1794/package_scripts": "0.0.12"
  }
}
